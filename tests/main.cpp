// tests/main.cpp
// Empty fallback — CMake links Catch2::Catch2WithMain, which provides main().
// This file exists so the test target always has at least one translation unit
// even if all other test files are temporarily excluded.
